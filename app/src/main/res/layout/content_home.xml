<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.alex.helloworld.Home"
    tools:showIn="@layout/activity_home">

    <GridLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="2"
        android:rowCount="2" >

        <Button
            android:id="@+id/sportart_button"
            android:layout_marginRight="4dp"
            android:text="Sportart"
            android:elevation="4dp"
            android:onClick="sportart"
            />

        <Button
            android:id="@+id/einladungen_button"
            android:text="Einladungen"
            android:onClick="einladungen"
            android:elevation="0dp"
            style="@style/Widget.AppCompat.Button" />

        <Button
            android:id="@+id/msg_button"
            android:layout_marginRight="4dp"
            android:text="Nachrichten"
            />

        <Button
            android:id="@+id/calendar_button"
            android:layout_marginRight="4dp"
            android:text="Calendar"
            android:onClick="buttonClick"
            style="@style/Widget.AppCompat.Button"
            />


    </GridLayout>

</RelativeLayout>
